<thead class="{{ $color }}">
  <tr>
    <th scope="{{ $scope }}">Name</th>
    <th scope="{{ $scope }}">Position</th>
    <th scope="{{ $scope }}">Office</th>
    <th scope="{{ $scope }}">Age</th>
    <th scope="{{ $scope }}">Start date</th>
    <th scope="{{ $scope }}">Salary</th>
  </tr>
</thead>

<!-- scope = 'col', '' -->
